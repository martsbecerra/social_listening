## Summary

- Reemplaza el análisis Anthropic-only (`src/anthropic.js`) por un pipeline LLM con JSON estructurado: muestra estable de comentarios, validación en Node, heurísticas de ruido/spam, registro de tipos de cuenta y reporte WhatsApp/CSV armado en código.
- Suma proveedor **OpenRouter** (además de Anthropic), telemetría de tokens/costo en API y UI, y un botón para compartir el reporte por WhatsApp.
- El monitoreo de `develop` queda intacto: `config/monitoring.json` se restauró al formato anidado que espera `loadConfig()`.

## Test plan

- [ ] `npm start` imprime el proveedor LLM activo (Anthropic u OpenRouter) y los límites `COMMENTS_LIMIT` / `COMMENTS_ANALYSIS_LIMIT`.
- [ ] Analizar un post de Instagram y verificar reporte + CSV de reclamos.
- [ ] Confirmar metadata: comentarios extraídos vs analizados, tokens y costo estimado.
- [ ] Probar `LLM_PROVIDER=openrouter` con un modelo que soporte `json_schema`.
- [ ] Botón de WhatsApp abre la app nativa con el texto del reporte (tope ~4000 chars).
- [ ] Solapa Monitoreo en vivo lista las cuentas (`jorgemacri`, `buenosaires`) y keywords de develop; "Correr ahora" scrapea fuentes.

## Notas para review

- Diff grande (~+2200 / −450). El núcleo está en `src/analyzeComments.js` + `src/llm/`.
- `.env.example` y README documentan `LLM_PROVIDER`, OpenRouter y costos.
